package main
/*
import (
	"os"
	"log"
	"encoding/json"
	"io/ioutil"
)
*/

func (host *WmHost) wm_json_read(){
	/*
	if len(os.Args) > 1 {
		log.Println(os.Args[1])
		data, err := ioutil.ReadFile(os.Args[1])
		if err != nil {
			log.Println("Failed to load Setting file.")
		} else {
			err = json.Unmarshal(data, &host.json_setting)
			if err != nil {
				log.Fatalln("Failed to parse Setting file.")
			}
		}
	}
	*/
}
